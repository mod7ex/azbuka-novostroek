{
    "private": true,
    "scripts": {
        "build": "rm -Rf .output && rm -Rf .nuxt/dist && nuxt build && pm2 restart node .output/server/index.mjs",
        "build:local": "rm -Rf .output && rm -Rf .nuxt/dist && nuxt build",
        "dev": "TAILWIND_MODE=watch nuxt dev",
        "generate": "nuxt generate",
        "preview": "nuxt preview",
        "postinstall": "nuxt prepare"
    },
    "devDependencies": {
        "@nuxtjs/apollo": "^5.0.0-alpha.5",
        "autoprefixer": "^10.4.12",
        "nuxt": "3.0.0-rc.9",
        "nuxt-icon": "^0.1.5",
        "postcss": "^8.4.17",
        "sass": "^1.54.9",
        "tailwindcss": "^3.1.8"
    },
    "dependencies": {
        "@apollo/client": "^3.7.2",
        "@vue/apollo-composable": "^4.0.0-beta.1",
        "graphql": "^16.6.0",
        "graphql-tag": "^2.12.6",
        "node-fetch-native": "^1.0.1",
        "vue-yandex-maps": "^0.12.3"
    }
}
